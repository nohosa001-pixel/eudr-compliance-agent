# Smithery.ai MCP Server Configuration
# https://smithery.ai/docs/build/external#config-schema

startCommand:
  type: "http"
  url: "https://eudragent.com/api/v1/mcp"
  configSchema:
    type: "object"
    properties:
      apiKey:
        type: "string"
        title: "API Key"
        description: "Optional EUDRAgent Pro API Key for enterprise quotas"
        x-from:
          header: "X-API-Key"
    required: []
exampleConfig:
  apiKey: "eudr_live_demo_key_2026"
